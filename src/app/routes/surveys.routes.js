import { Router } from "express";


const router = Router();

//Routes here

router.get("/",)
router.post("/",)
router.delete("/:id",)
router.patch("/:id",)
router.get("/:id",)


export default router;